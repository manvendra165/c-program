#include<stdio.h>

int main(int argc, char const *argv[])
{
    /* code */
    int num;
    printf("enter the no\n");
    scanf("%d",&num);
    if(num%2==0)
    printf("no is even");
    else
    printf("no is odd");
    
    return 0;
}

